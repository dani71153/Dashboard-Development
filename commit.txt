feat(acceleration): grid 2×2 y canvas individual por rueda

- Ajusta sección de aceleraciones a un grid 2×2 con cuatro canvas separados
  (ruedas 1–4), manteniendo el orden de leyenda solicitado.
- prueba.html (line 405): nueva estructura de tarjetas y canvas por rueda.
- dashboard.js (line 237): almacena charts por rueda en el estado de navegación.
- dashboard.js (line 4922–5042): crea/limpia cada Chart individual, calcula
  aceleración por rueda ((vel_actual – vel_previa) / dt) y actualiza datasets.
- Pruebas con: node -e "new Function(require('fs').readFileSync('dashboard.js','utf8'))"
