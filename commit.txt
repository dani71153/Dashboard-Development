Implementé una vista XY manual para el mapa y herramientas para anotar poses:

Sustituí la integración ROS2D por un canvas propio que dibuja retícula, ejes y la flecha del robot, con redimensionamiento y actualización vía /pose (dashboard.js:229, dashboard.js:367, dashboard.js:546).

Añadí formulario y listado de puntos de interés con colores, soporte para orientación opcional y botón “Usar pose actual” que captura la pose activa (prueba.html:458, dashboard.css:252, dashboard.js:472, dashboard.js:900).

Ajusté el placeholder para que no bloquee la interacción y se oculte automáticamente al recibir datos o al añadir puntos (dashboard.js:151, dashboard.css:409).

Tests: node --check dashboard.js